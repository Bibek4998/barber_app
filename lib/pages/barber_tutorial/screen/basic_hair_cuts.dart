import 'package:flutter/material.dart';

class BasicHairCuts extends StatelessWidget {
  const BasicHairCuts({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: Text('Basic Hair Cuts')));
  }
}
