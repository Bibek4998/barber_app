import 'package:flutter/material.dart';

class HairColoring extends StatelessWidget {
  const HairColoring({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
