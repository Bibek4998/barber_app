import 'package:flutter/material.dart';

class FaceWashing extends StatelessWidget {
  const FaceWashing({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: Text('Face Washing')));
  }
}
